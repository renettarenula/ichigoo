import React from "react";
import { Link as RouteLink } from "react-router-dom";
import { IchigooLink } from "ichigoo";

const LinkRecorder = IchigooLink(RouteLink);

const Linking = (props) => {
  return <LinkRecorder {...props} />;
};

export default Linking;
